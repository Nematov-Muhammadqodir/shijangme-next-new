export enum MemberType {
  USER = "USER",
  ADMIN = "ADMIN",
  VENDOR = "VENDOR",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}

export enum MemberAuthType {
  PHONE = "PHONE",
  EMAIL = "EMAIL",
  TELEGRAM = "TELEGRAM",
}
